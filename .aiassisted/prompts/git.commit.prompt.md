# Variables

```
$ROOT_PROJECT = $(git rev-parse --show-toplevel)
$AIASSISTED_DIR = $ROOT_PROJECT/.aiassisted/
$INSTRUCTION = $AIASSISTED_DIR/instructions/conventional-commits.instructions.md
```

# References

You need to load, read, and observe defined instructions at: `$INSTRUCTION`.

# Instruction

You need to commit the changes to the repository, by following the conventional commits, based on defined detailed instructions at: `$INSTRUCTION`.